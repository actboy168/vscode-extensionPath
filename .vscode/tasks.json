{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "echo ${command:extensionPath}",
            "type": "shell",
            "command": "echo ${command:extensionPath}"
        }
    ]
}
